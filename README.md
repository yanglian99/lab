# Codex

Codex is a lightweight static landing page prototype for an AI-native product workspace.

## Run locally

Because the app is a static site, you can serve it with any simple file server. For example:

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000> in your browser.
