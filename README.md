# 微信跳一跳（简化版）

一个使用原生 `HTML + CSS + JavaScript` 写的网页小游戏。

## 玩法

- 按住 **空格**（或鼠标/手指按住画布）蓄力。
- 松开后角色会向前跳跃。
- 落在方块上得分，越靠近中心分数越高。
- 掉落则游戏结束，可点击“重新开始”。

## 本地运行

直接打开 `index.html` 即可，或使用一个静态服务器，例如：

```bash
python3 -m http.server 8000
```

然后访问 <http://localhost:8000>。
