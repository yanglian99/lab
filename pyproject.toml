[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "messaging-codex-prototype"
version = "0.1.0"
description = "Channel-agnostic messaging to Codex prototype"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
